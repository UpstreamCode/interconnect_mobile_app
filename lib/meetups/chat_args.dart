// You can pass any object to the arguments parameter.
// In this example, create a class that contains a customizable
// title and message.
class ChatArgs {
  final String name;
  final String message;

  ChatArgs(this.name, this.message);
}
